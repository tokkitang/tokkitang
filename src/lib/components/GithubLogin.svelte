<script lang="ts">
    // import { onMount } from 'svelte'

    // let fontAwesome
	// onMount(async () => {
	// 	fontAwesome = (await import('https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.1/css/all.min.css')).default
	// })

    // let bootstrapCSS
	// onMount(async () => {
	// 	bootstrapCSS = (await import('https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css')).default
	// })

    // let bootstrapJS
	// onMount(async () => {
	// 	bootstrapJS = (await import('https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js')).default
	// })

	// let bootstrapSocial
	// onMount(async () => {
	// 	bootstrapJS = (await import('https://cdnjs.cloudflare.com/ajax/libs/bootstrap-social/4.9.2/bootstrap-social.min.css')).default
	// })
</script>

<main>
    <a id="github-button" class="btn btn-block btn-social btn-github">
        <i class="fa fa-github"></i> Sign in with GitHub
    </a>
</main>

<style>

</style>